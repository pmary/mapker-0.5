/**
 * In Cucumber, this.Before will be called before ever scenario.
 * By calling the reset method in the mirror fixture before every scenario,
 * we can ensure the SUT is completely clean.
 */
/*module.exports = function () {
  this.Before(function () {
    return this.server.call('reset');
  });
});*/
