// Test user account creation
/*if (Meteor.isClient) {
  testAsyncMulti('Test login method', [
    function (test, excepted) {
      Meteor.call('mapker:users/userCreateAccount', expect(function (err, res) {
        test.instanceOf(err, Meteor.Error);
      }));
    }
  ]);
}
*/
